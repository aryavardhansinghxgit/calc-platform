import { CalculatorModuleDefinition } from "../types";
import { PERCENTAGE_CALCULATOR } from "./percentage";

export const MATH_CALCULATORS: CalculatorModuleDefinition[] = [
  PERCENTAGE_CALCULATOR,
];

export default MATH_CALCULATORS;
