import { CalculatorModuleDefinition } from "../../types";
import { dti_calculatorConfig } from "@/app/calculators/dti-calculator/config";

export const DTI_CALCULATOR: CalculatorModuleDefinition = dti_calculatorConfig;

export default DTI_CALCULATOR;
