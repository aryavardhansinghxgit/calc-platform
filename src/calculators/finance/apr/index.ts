import { CalculatorModuleDefinition } from "../../types";
import { apr_calculatorConfig } from "@/app/calculators/apr-calculator/config";

export const APR_CALCULATOR: CalculatorModuleDefinition = apr_calculatorConfig;

export default APR_CALCULATOR;
