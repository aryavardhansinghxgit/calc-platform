import { CalculatorModuleDefinition } from "../../types";
import { cdConfig } from "@/app/calculators/cd-calculator/config";

export const CD_CALCULATOR: CalculatorModuleDefinition = cdConfig;

export default CD_CALCULATOR;
