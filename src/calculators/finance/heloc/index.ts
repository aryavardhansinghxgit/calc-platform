import { CalculatorModuleDefinition } from "../../types";
import { helocConfig } from "@/app/calculators/heloc-calculator/config";

export const HELOC_CALCULATOR: CalculatorModuleDefinition = helocConfig;

export default HELOC_CALCULATOR;
