import { CalculatorModuleDefinition } from "../../types";
import { financeConfig } from "@/app/calculators/finance-calculator/config";

export const FINANCE_CALCULATOR_SUITE: CalculatorModuleDefinition = financeConfig;

export default FINANCE_CALCULATOR_SUITE;
