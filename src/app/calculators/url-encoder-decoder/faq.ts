import { CalculatorFAQ } from "@/lib/calculator-engine/types";

export const url_encoder_decoderFaqs: CalculatorFAQ[] = [
  {
    "question": "Why do URLs need encoding?",
    "answer": "URLs can only contain ASCII characters. Reserved characters like spaces or & must be percent-encoded."
  }
];
