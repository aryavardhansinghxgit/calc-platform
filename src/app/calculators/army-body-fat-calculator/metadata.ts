import { Metadata } from "next";
import { generateCalculatorMetadata } from "@/lib/seo-helpers";

export const army_body_fat_calculatorMetadata: Metadata = generateCalculatorMetadata({
  title: "Army Body Fat Calculator 2026: WHtR & Army Body Composition Standard",
  description:
    "Use the 2026 Army Body Composition Calculator to calculate waist-to-height ratio (WHtR), check the 0.55 Army standard, and understand compliant and non-compliant results.",
  slug: "army-body-fat-calculator",
});

export default army_body_fat_calculatorMetadata;
