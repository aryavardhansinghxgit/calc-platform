import { Metadata } from "next";
import { generateCalculatorMetadata } from "@/lib/seo-helpers";

export const army_body_fat_calculatorMetadata: Metadata = generateCalculatorMetadata({
  title: "Army Body Fat Calculator — Free Online Health Calculator",
  description: "Determine body fat compliance according to U.S. Army Standards (AR 600-9 tape test).",
  slug: "army-body-fat-calculator",
});
