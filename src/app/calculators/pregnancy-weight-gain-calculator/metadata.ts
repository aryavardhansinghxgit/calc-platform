import { Metadata } from "next";
import { generateCalculatorMetadata } from "@/lib/seo-helpers";

export const pregnancy_weight_gain_calculatorMetadata: Metadata = generateCalculatorMetadata({
  title: "Pregnancy Weight Gain Calculator — Free Online Health Calculator",
  description: "Calculate healthy weight gain targets by week of pregnancy based on Institute of Medicine (IOM) guidelines.",
  slug: "pregnancy-weight-gain-calculator",
});
