import { Metadata } from "next";
import { generateCalculatorMetadata } from "@/lib/seo-helpers";

export const pregnancy_conception_calculatorMetadata: Metadata = generateCalculatorMetadata({
  title: "Pregnancy Conception Calculator — Free Online Health Calculator",
  description: "Estimate the probable conception date and fertile window based on due date or last period.",
  slug: "pregnancy-conception-calculator",
});
